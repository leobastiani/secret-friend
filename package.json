{
  "name": "secret-friend",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "prepare": "yarn build",
    "prepublishOnly": "yarn test && yarn lint",
    "preversion": "yarn lint",
    "version": "yarn format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "version": "1.0.1",
  "files": [
    "lib/**/*"
  ],
  "description": "Generate secret friends party list",
  "repository": "https://github.com/leobastiani/secret-friend",
  "author": "Leonardo Guarnieri de Bastiani <leogbastiani@gmail.com>",
  "license": "MIT",
  "main": "./lib/main.js",
  "types": "./lib/main.d.ts",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "jest": "^27.4.5",
    "prettier": "^2.5.1",
    "ts-jest": "^27.1.1",
    "typescript": "^4.5.4"
  }
}
